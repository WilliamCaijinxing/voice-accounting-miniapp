# 语音记账小程序 — 架构总览文档

## 一、项目概述

**产品定位**：通过语音一句话输入完成记账，自动识别金额、分类、日期，支持日/月/分类多维统计。

**核心卖点**：「按住说话，一句话记账」——相比手动输入同类产品，记账速度提升 5-10 倍。

**目标用户**：个人日常消费者，追求记录效率的用户，碎片时间记账场景。

---

## 二、技术选型与架构决策

| 决策维度 | 选择 | 理由 |
|----------|------|------|
| 后端方案 | **微信云开发 (Cloud Base)** | 零服务器运维，快速上线，自动扩缩容，免费额度充足 |
| 语音识别 | **微信同声传译插件 (WechatSI)** | 免费、内置微信生态、中文识别准确率高、无需第三方依赖 |
| 语音解析 | **自研 NLP 云函数 (关键词匹配)** | MVP 阶段足够；后续可接入大模型提升准确率 |
| 数据库 | **云开发文档数据库** | MongoDB-like，天然支持按 `_openid` 隔离用户数据 |
| 前端框架 | **原生 WXML/WXSS/JS** | 包体积最小，无框架开销，符合小程序的 2MB 限制 |
| UI 组件 | **TDesign Miniprogram（可选）** | 企业级组件库，后续功能扩展时引入 |
| 状态管理 | **app.globalData + 页面级 state** | MVP 轻量，避免引入 MobX 增加包体积 |

---

## 三、项目结构

```
voice-accounting-miniapp/
├── miniprogram/
│   ├── app.js                    # 应用入口：云开发初始化、自动登录、全局分类配置
│   ├── app.json                  # 页面路由、tabBar、插件声明(WechatSI)、权限声明
│   ├── app.wxss                  # 全局设计 Token（CSS 变量体系）
│   ├── project.config.json       # 项目配置
│   ├── pages/
│   │   ├── index/                # 首页仪表盘
│   │   ├── record/               # 语音记账核心页（核心交互）
│   │   └── statistics/           # 统计报表页
│   ├── components/
│   │   └── expense-card/         # 统一账目卡片组件
│   └── utils/
│       ├── cloud.js              # 云函数调用封装（expenseAPI / statsAPI / voiceAPI）
│       └── util.js               # 通用工具（金额格式化、日期处理、分类映射）
└── cloudfunctions/
    ├── parseVoice/               # NLP 解析：语音文本 → {金额, 分类, 日期, 描述}
    ├── expenseCRUD/              # 账目增删改查 + 分页
    ├── statistics/               # 聚合统计：今日/月度/分类/趋势
    └── userLogin/                # 静默登录
```

---

## 四、核心数据流

```
用户按住说话
    │
    ▼
RecorderManager.start()  ── 录制 15s 内音频
    │
    ▼
WechatSI 同声传译插件  ── 实时语音→文本
    │
    ▼
Cloud: parseVoice        ── NLP 解析
    ├── 正则提取金额（支持"35元""花了25""12.5块"等口语化表达）
    ├── 关键词匹配分类（"外卖""午餐"→餐饮，"打车""地铁"→交通）
    ├── 日期识别（"今天""昨天""前天"及"MM月DD日"）
    └── 描述提取（去除金额/分类词的剩余文本）
    │
    ▼
用户确认页面  ── 可修改分类、日期、描述
    │
    ▼
Cloud: expenseCRUD.create()  ── 写入 expenses 集合
    │
    ▼
首页/统计页  ── Cloud: statistics.today/monthly/category/daily
    │
    ▼
实时渲染汇总金额、分类占比、每日趋势
```

---

## 五、数据库设计

### expenses 集合

| 字段 | 类型 | 说明 |
|------|------|------|
| `_id` | string | 自动生成 |
| `_openid` | string | 用户标识（云开发自动注入） |
| `amount` | number | 金额（单位：分，避免浮点精度问题） |
| `type` | string | `'expense'` / `'income'` |
| `categoryId` | string | 分类 ID（如 `food`, `transport`） |
| `categoryName` | string | 分类中文名 |
| `date` | string | `YYYY-MM-DD` |
| `description` | string | 备注描述 |
| `createdAt` | Date | 创建时间 |
| `updatedAt` | Date | 更新时间 |

**索引**：`_openid + date` 复合索引（覆盖所有查询场景）

### categories 集合（预留）

| 字段 | 类型 | 说明 |
|------|------|------|
| `_openid` | string | 用户标识 |
| `name` | string | 分类名称 |
| `icon` | string | 图标标识 |
| `type` | string | expense/income |
| `isDefault` | boolean | 是否系统预设 |

---

## 六、NLP 解析引擎设计

### 分类关键词库（13 个预设分类）

系统在 `app.js` 的 `globalData.categoryConfig` 中预定义了 13 个分类及对应关键词：

| 支出分类 | 关键词示例 | 收入分类 | 关键词示例 |
|----------|-----------|----------|-----------|
| 餐饮 🍜 | 吃饭、外卖、午餐、火锅、奶茶 | 工资 💰 | 工资、薪水、奖金、年终奖 |
| 交通 🚗 | 打车、地铁、公交、加油、高铁 | 兼职 💼 | 兼职、外快、副业 |
| 购物 🛍️ | 衣服、超市、网购、数码 | 理财 📈 | 利息、股票、基金、分红 |
| 居家 🏠 | 房租、水电、物业、宽带 | 退款 ↩️ | 退款、报销、返现 |
| 娱乐 🎮 | 电影、KTV、游戏、旅游 | 其他收入 💵 | 兜底 |
| 医疗 💊 | 看病、药、医院、体检 | | |
| 教育 📚 | 学费、培训、课程 | | |
| 人情 🎁 | 红包、结婚、生日、礼物 | | |
| 其他 📌 | 兜底 | | |

### 金额提取策略（多条正则兜底）

```
"午餐花了35块"   → 35 × 100 = 3500 分
"打车25元"      → 25 × 100 = 2500 分
"超市128.5"     → 128.5 × 100 = 12850 分
"昨天吃饭用了50"  → 50 × 100 = 5000 分（日期：昨天）
```

### 准确率预估

- MVP 关键词匹配：约 80% 首猜准确率
- 用户可在确认页一键修正分类
- 后续升级：接入大模型（如混元/文心）可提升至 95%+

---

## 七、页面设计

### 页面 1：首页仪表盘 (index)

**视觉层级**：
1. 绿色渐变英雄卡片：今日支出/收入/笔数 → 空状态时有友好引导文案
2. 分类彩色占比条（视觉上快速感知消费结构）
3. 巨大语音入口按钮（绿色圆形 + 悬浮阴影，最显眼的 CTA）
4. 本月概览卡片（月支出/收入/结余）
5. 最近账单列表（支持点击删除）

### 页面 2：语音记账页 (record) — 核心交互

**状态机设计**（4 个阶段）：

```
idle → recording → parsing → confirm → idle
  ↑                                  │
  └────────── 重新录入 ←──────────────┘
```

| 阶段 | UI 表现 | 交互 |
|------|---------|------|
| idle | 示例提示 + 巨大语音按钮 | 按住开始录音 |
| recording | 波形动画 + 计时器 + 脉冲光圈 | 松手结束 |
| parsing | 转圈 + "正在识别分类..." | 等待 |
| confirm | 金额/分类/日期/描述 → 可编辑 | 确认保存或重新录入 |

**降级方案**：页面底部提供「手动记账」入口，输入文本走同一套 NLP 解析流程。

### 页面 3：统计报表 (statistics)

- 月份切换导航（‹ 2026年6月 ›）
- 月支出/月收入/日均支出
- 分类饼图 + 图例列表
- 每日趋势柱状图（横向滚动）
- 空状态：本月暂无数据引导

---

## 八、快速上线路径（5 步）

### Step 1：注册小程序 + 开通云开发
- 注册小程序账号 → 获取 AppID
- 在开发者工具中开通云开发 → 创建环境（免费额度：2GB 存储、5GB 数据库、5GB 云函数流量）
- 替换 `app.js` 中的 `env` 和 `project.config.json` 中的 `appid`

### Step 2：部署云函数
- 在开发者工具中右键每个云函数目录 → 「上传并部署：云端安装依赖」
- 创建数据库集合：`expenses`（索引：`_openid` + `date`）

### Step 3：配置语音插件
- 在微信公众平台 → 设置 → 第三方设置 → 插件管理 → 添加「微信同声传译」插件
- `app.json` 中已声明插件配置，无需额外修改

### Step 4：真机测试
- 扫码预览 → 测试语音授权 → 测试录音 → 测试识别 → 测试保存
- 注意：同声传译插件必须在真机上测试，开发者工具不支持

### Step 5：提交审核
- 确保 `app.json` 中 `requiredPrivateInfos` 和 `permission` 声明完整
- 隐私协议中说明录音权限用途
- 首次提交通常 1-3 个工作日审核

---

## 九、审核注意事项

| 常见拒绝原因 | 如何避免 |
|-------------|---------|
| 隐私政策不完整 | 在小程序后台设置中填写完整的隐私保护指引 |
| 录音权限未声明用途 | `permission.scope.record.desc` 中明确说明「语音记账」用途 |
| 类目选择不当 | 选择「工具 > 记账」类目，无需特殊资质 |
| 页面空白/功能未完成 | 确保所有 tab 页面都有完整内容，空状态要有引导 |

---

## 十、用户获取策略

1. **分享裂变**：首页/记账成功后可分享「本月已节省 ¥XXX」，带打开小程序的 path
2. **订阅消息**：每日晚 8 点推送「今日账单汇总」（需用户主动授权）
3. **公众号联动**：如有公众号，在文章中嵌入小程序卡片
4. **微信群传播**：支持分享到群，附带「一起来记账」文案
5. **视频号嵌入**：如有视频号，在短视频中挂载小程序链接

---

## 十一、后续迭代方向

| 优先级 | 功能 | 难度 |
|--------|------|------|
| P0 | 手动记账完善（金额输入 + 分类选择） | 低 |
| P0 | 预算设置与超支提醒 | 中 |
| P1 | 大模型 NLP 升级（提升分类准确率） | 中 |
| P1 | 数据导出（CSV/图片） | 低 |
| P1 | 多人共享账本（家庭记账） | 高 |
| P2 | 银行卡/支付宝账单导入 | 高 |
| P2 | 消费趋势预测与建议 | 高 |
| P2 | 订阅消息每日推送 | 中 |
