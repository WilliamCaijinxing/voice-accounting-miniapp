# 语音记账小程序 — 部署上线指南

> 从零到上线，预计耗时 **2-3 小时**（不含审核等待）

---

## 前置准备清单

| 项目 | 说明 | 获取方式 |
|------|------|---------|
| 微信小程序账号 | 个人或企业均可 | [mp.weixin.qq.com](https://mp.weixin.qq.com/) 注册 |
| AppID | 小程序唯一标识 | 注册后在后台「开发 → 开发管理 → 开发设置」获取 |
| 微信开发者工具 | 开发和上传代码 | [官方下载](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html) |
| 云开发环境 | 后端服务 | 开发者工具中一键开通 |

---

## 第一步：注册小程序账号

1. 打开 [微信公众平台](https://mp.weixin.qq.com/)，点击「注册」
2. 选择「小程序」类型
3. 填写邮箱（未注册过微信公众号的邮箱）
4. 完成邮箱激活和信息登记
   - **个人主体**：身份证 + 微信扫码验证
   - **企业主体**：营业执照 + 管理员信息
5. 注册成功后，进入后台 → 「开发 → 开发管理 → 开发设置」
6. 记下你的 **AppID**（形如 `wx1234567890abcdef`）

> **类目选择**：个人主体选「工具 → 效率」，企业主体可选「工具 → 记账」。记账类目无需特殊资质。

---

## 第二步：配置项目

### 2.1 替换 AppID

打开项目根目录的 `project.config.json`，将 `appid` 替换为你的真实 AppID：

```json
{
  "appid": "wx你的真实AppID",
  "miniprogramRoot": "miniprogram/",
  "cloudfunctionRoot": "cloudfunctions/"
}
```

### 2.2 导入项目

1. 打开微信开发者工具
2. 点击「导入项目」（或「打开项目」）
3. 项目目录选择 `voice-accounting-miniapp/`（**根目录，包含 project.config.json**）
4. 开发者工具会根据 `miniprogramRoot` 自动找到 `miniprogram/` 下的 `app.json`
5. AppID 填入你的真实 AppID
6. 点击确定

> **注意**：`project.config.json` 已从 `miniprogram/` 子目录移到根目录，并添加了 `miniprogramRoot` 和 `cloudfunctionRoot` 配置。这是微信云开发项目的标准结构，确保开发者工具能正确找到 app.json 和云函数目录。

### 2.3 开通云开发

1. 开发者工具顶部菜单 → 「云开发」按钮（云朵图标）
2. 点击「开通」→ 同意协议
3. 输入环境名称（如 `voice-accounting-prod`）
4. 选择「基础版（免费）」
5. 创建完成后，记下 **环境 ID**（形如 `voice-accounting-0g1234`）

### 2.4 替换云环境 ID

打开 `miniprogram/app.js`，第 7 行：

```javascript
wx.cloud.init({
  env: 'voice-accounting-0g1234', // ← 替换为你的云环境ID
  traceUser: true,
});
```

---

## 第三步：创建数据库集合

> **快速方式**：项目已提供可直接导入的 JSON 数据文件，位于 `database/` 目录。

### 方式一：导入预置数据文件（推荐）

1. 开发者工具 → 云开发控制台 → 数据库
2. 点击「添加集合」，创建 `expenses` 集合
3. 进入 `expenses` 集合 → 点击「导入」→ 选择 `database/expenses.json`
4. 导入格式选择 **JSON Lines**（每行一条记录）
5. 重复步骤 2-4 创建 `users` 集合并导入 `database/users.json`
6. 创建 `budgets` 集合并导入 `database/budgets.json`（含 2026-08 月预算等示例数据，正式使用前可在预算设置页重新覆盖）
7. 创建 `ledgers` 集合并导入 `database/ledgers.json`（共享账本示例，正式使用前请删除示例数据）
8. 设置权限：
   - `expenses` / `users` / `budgets` → **「仅创建者可读写」**
   - `ledgers` → **「所有用户可读，仅创建者可写」**（共享账本需被非创建者成员读取，跨用户写入统一走 `groupCRUD` 云函数）

> 详细字段说明和索引配置参见 `database/README.md`

### 方式二：手动创建集合（不导入数据）

在云开发控制台中逐一手动创建：

### expenses 集合（账目数据）

| 字段 | 类型 | 说明 |
|------|------|------|
| `_openid` | string | 用户标识（自动注入） |
| `amount` | number | 金额（单位：分） |
| `type` | string | `expense` 或 `income` |
| `categoryId` | string | 分类 ID |
| `categoryName` | string | 分类名称 |
| `date` | string | `YYYY-MM-DD` |
| `description` | string | 备注 |
| `createdAt` | Date | 创建时间 |
| `updatedAt` | Date | 更新时间 |

**权限设置**：改为「仅创建者可读写」

**索引**（提升查询性能）：
- 字段 `_openid`（升序）+ `date`（降序）→ 复合索引

### users 集合（用户信息）

| 字段 | 类型 | 说明 |
|------|------|------|
| `_openid` | string | 用户标识 |
| `nickname` | string | 昵称 |
| `avatar` | string | 头像 URL |
| `createdAt` | Date | 注册时间 |
| `lastLoginAt` | Date | 最后登录 |

**权限设置**：改为「仅创建者可读写」

### budgets 集合（预算管理）

| 字段 | 类型 | 说明 |
|------|------|------|
| `_openid` | string | 用户标识 |
| `type` | string | `month`（月度）/ `year`（年度） |
| `ym` | string | 月份标识 `YYYY-MM`（年度为 `YYYY-00`） |
| `year` | number | 年份 |
| `month` | number | 月份（年度为 0） |
| `amount` | number | 预算金额（单位：分） |
| `createdAt` | Date | 创建时间 |
| `updatedAt` | Date | 更新时间 |

**权限设置**：改为「仅创建者可读写」

**索引**：
- 字段 `_openid`（升序）+ `ym`（升序）→ 复合索引

### ledgers 集合（共享账本 / 群组）

| 字段 | 类型 | 说明 |
|------|------|------|
| `name` | string | 账本名称 |
| `ownerOpenid` | string | 创建者 openid |
| `members` | array | 成员列表 `[{ openid, nickname, avatar, role, joinedAt }]` |
| `inviteCode` | string | 6 位邀请码（唯一索引） |
| `createdAt` | Date | 创建时间 |

**权限设置**：改为「**所有用户可读，仅创建者可写**」

**索引**：
- 字段 `inviteCode`（升序）→ 唯一索引（加入时按邀请码查询）
- 字段 `ownerOpenid`（升序）→ 普通索引（查询我创建的账本）

> ⚠️ 共享账本内成员的账目（`expenses`/`budgets`）仍保持「仅创建者可读写」，**跨用户的数据读写一律通过云函数 `groupCRUD` / `expenseCRUD` 完成**，前端不可直连数据库查询他人账目。

---

## 第四步：部署云函数

项目共有 **9 个云函数**需要部署：

```
cloudfunctions/
├── userLogin/      ← 用户登录/注册（静默登录 + 资料更新）
├── parseVoice/     ← NLP 语音文本解析
├── expenseCRUD/    ← 账目增删改查（支持 ledgerId 多账本隔离）
├── statistics/     ← 统计聚合（日历 + 日/周/月/年趋势，支持 ledgerId）
├── voiceASR/       ← 语音识别（备用方案）
├── voiceToText/    ← 腾讯云语音识别（推荐，ASR+NLP 一体化：录音 base64 直传，一次调用返回结构化账目）
├── budgetCRUD/     ← 预算管理（设置/查询月度年度预算，支持 ledgerId）
├── groupCRUD/      ← 共享账本管理（创建/加入/成员/邀请码/退出/移除/解散）
└── exportData/     ← 账本导出（生成 CSV 上传云存储）
```

### 部署步骤（每个云函数重复一次）

1. 在开发者工具左侧文件树中，展开 `cloudfunctions/` 目录
2. **右键**点击云函数文件夹（如 `userLogin`）
3. 选择 **「上传并部署：云端安装依赖」**
4. 等待上传完成（控制台会显示 `上传成功` 提示）
5. 对其余 8 个云函数重复上述操作

> 首次部署每个云函数约需 30-60 秒（云端安装依赖）。`voiceToText` 云函数需要安装 `tencentcloud-sdk-nodejs`，首次部署可能需要 1-2 分钟。

### 验证云函数

部署完成后，在「云开发控制台 → 云函数」中应看到 9 个函数，状态均为「已部署」。

---

## 第五步：配置腾讯云语音识别（可选但推荐）

> 语音识别是语音记账的核心功能。如果不配置，录音后会提示用户使用「手动输入」。

### 5.1 开通腾讯云语音识别服务

1. 访问 [腾讯云语音识别控制台](https://console.cloud.tencent.com/asr)
2. 登录（使用与微信云开发相同的腾讯云账号）
3. 点击「开通服务」
4. 选择「一句话识别」或「录音文件识别」
5. 完成实名认证（如果还没做）

> 腾讯云语音识别每月有 **10000 次免费调用额度**，个人记账完全够用。

### 5.2 获取 API 密钥

1. 访问 [腾讯云 API 密钥管理](https://console.cloud.tencent.com/cam/capi)
2. 点击「新建密钥」
3. 记下 **SecretId** 和 **SecretKey**

> **安全提示**：密钥不要写在代码里，通过环境变量注入。

### 5.3 配置云函数环境变量

1. 开发者工具 → 云开发控制台 → 云函数
2. 找到 `voiceToText` 函数，点击「版本与配置」
3. 选择「函数配置」标签页
4. 找到「环境变量」，点击「编辑」
5. 添加两个变量：

| 变量名 | 值 |
|--------|-----|
| `TENCENT_SECRET_ID` | 你的 SecretId |
| `TENCENT_SECRET_KEY` | 你的 SecretKey |

6. 保存后，重新部署 `voiceToText` 云函数（右键 → 上传并部署：云端安装依赖）

### 5.4 识别速度优化（推荐）

目标：说完话到出结果控制在 **1 秒内**。项目已内置两项优化：

1. **云函数预热**：进入记账页、按下录音时，会提前调用 `voiceToText` 的 `warmup` 接口拉起云函数实例，消除冷启动（可省 300~800ms，这是耗时大头）。
2. **ASR 参数优化**：关闭脏词/语气词/标点过滤；开启 `ConvertNumMode`，口语中文数字自动转阿拉伯数字（"两块"→"2块"），既提速也提升金额解析准确率。

如需进一步提速，在云开发控制台 → 云函数 → `voiceToText` → 版本与配置 → 函数配置，把**内存调至 512MB**（更大内存配额会分配更强 CPU，冷启动与执行都更快）。

> 排查慢在哪：真机预览时看控制台日志 `[Record][perf] 读音频 Xms | 识别+解析 Yms | 总计 Zms`，以及云函数日志里的 `[voiceToText] ASR 耗时`。

---

## 第六步：配置 TabBar 图标

`app.json` 引用了 8 个图标文件（已随项目提供，位于 `miniprogram/images/`，由 `generate_icons.py` 脚本生成）：

| 文件名 | 用途 | 建议尺寸 |
|--------|------|---------|
| `tab-home.png` / `tab-home-active.png` | 首页 | 81x81px |
| `tab-record.png` / `tab-record-active.png` | 记账 | 81x81px |
| `tab-stats.png` / `tab-stats-active.png` | 统计 | 81x81px |
| `tab-mine.png` / `tab-mine-active.png` | 我的 | 81x81px |

> 如需重新生成或更换风格，可运行 `python generate_icons.py`（需 Pillow），或按下方方案手动替换。

### 快速生成方案

- **方案 A**：在 [iconfont.cn](https://www.iconfont.cn/) 搜索「首页/记账/统计」图标，下载 PNG，用 Photoshop/在线工具改为 81x81px
- **方案 B**：使用 TDesign 图标库（[tdesign.tencent.com](https://tdesign.tencent.com/miniprogram/icon)），下载对应图标
- **方案 C**（最快）：临时移除图标配置，使用纯文字 TabBar

如果选择方案 C（临时），修改 `app.json` 的 tabBar：

```json
{
  "tabBar": {
    "color": "#999999",
    "selectedColor": "#07C160",
    "backgroundColor": "#FFFFFF",
    "borderStyle": "black",
    "list": [
      { "pagePath": "pages/index/index", "text": "首页" },
      { "pagePath": "pages/record/record", "text": "记账" },
      { "pagePath": "pages/statistics/statistics", "text": "统计" }
    ]
  }
}
```

> 提审前建议补上图标，纯文字 TabBar 虽然能过审但体验略差。

---

## 第七步：配置隐私协议

### 7.1 后台配置

1. 登录微信公众平台
2. 「设置 → 基本设置 → 服务内容声明 → 用户隐私保护指引」
3. 点击「配置」
4. 勾选「麦克风」 → 用途填写「语音录入记账内容」
5. 提交并等待审核（通常 1-2 小时）

### 7.2 确认权限声明

`app.json` 中已有录音权限声明：

```json
{
  "permission": {
    "scope.record": {
      "desc": "需要使用麦克风录制语音记账"
    }
  }
}
```

---

## 第八步：真机测试

### 8.1 扫码预览

1. 开发者工具顶部点击「预览」按钮
2. 用手机微信扫描弹出的二维码
3. 小程序在手机上打开

### 8.2 测试清单

| 测试项 | 操作 | 预期结果 |
|--------|------|---------|
| 登录 | 打开小程序 | 自动静默登录，无报错 |
| 语音授权 | 首次按住录音按钮 | 弹出麦克风授权弹窗 |
| 语音识别 | 按住说「午餐花了35块」 | 识别为文字「午餐花了35块」 |
| NLP 解析 | 识别完成后 | 显示金额35、分类餐饮 |
| 保存账目 | 点击「确认保存」 | 提示保存成功 |
| 首页刷新 | 返回首页 | 今日支出显示 ¥35 |
| 统计页面 | 切换到统计 Tab | 顶部「日/周/月/年」四个标签可切换 |
| 日历视图 | 统计 Tab → 日 | 日历显示每日收支，点日期弹出当天明细 |
| 周趋势 | 统计 Tab → 周 | 近12周柱状图 + 全年52周（可切年份） |
| 月趋势 | 统计 Tab → 月 | 最近12个月柱状图，可向前翻页，点柱子看该月分类 |
| 年趋势 | 统计 Tab → 年 | 历年消费对比柱状图 |
| 我的页面 | 底部第 4 个 Tab | 头像昵称、今日/本月支出、预算进度、功能入口 |
| 完善资料 | 我的 → 点击头像 | 微信授权或手动输入昵称，资料保存成功 |
| 手动记账 | 点击「手动记账」 | 可手动输入（如果已实现） |
| 共享账本入口 | 我的 → 共享账本 / 首页「管理」 | 进入账本管理页 |
| 创建共享账本 | 账本页 → 创建账本 | 输入名称，自动成为创建者并切换为该账本 |
| 邀请码加入 | 账本页 → 邀请码加入 | 输入 6 位邀请码，加入后切换为该账本 |
| 分享卡片加入 | 账本详情 → 分享给好友 | 好友点开分享卡片自动加入 |
| 成员/邀请码 | 账本详情 | 查看成员、复制/重置邀请码（仅创建者） |
| 切换账本 | 首页顶部账本胶囊 | 切换到个人/共享账本，统计随之联动 |
| 退出/解散 | 账本详情 | 成员退出、创建者解散（级联删除账目与预算） |

> **注意**：语音识别功能需要在真机上测试，开发者工具模拟器不支持录音。

### 8.3 常见问题排查

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| 云函数调用报错 | 云环境 ID 未替换 | 检查 `app.js` 中的 `env` |
| 录音无反应 | 模拟器不支持 | 用真机预览测试 |
| 插件加载失败 | 未在后台添加插件 | 完成第五步 |
| 保存报权限错误 | 数据库权限未设置 | 改为「仅创建者可读写」 |
| 首页空白 | 云函数未部署 | 完成第四步 |

---

## 第九步：提交审核

### 9.1 上传代码

1. 开发者工具顶部点击「上传」按钮
2. 版本号填 `1.0.0`
3. 项目备注填「首次上线版本」
4. 点击确定

### 9.2 提交审核

1. 登录微信公众平台
2. 「管理 → 版本管理」
3. 在「开发版本」中找到刚上传的版本
4. 点击「提交审核」
5. 填写审核信息：
   - **服务类目**：工具 → 效率（个人）/ 工具 → 记账（企业）
   - **功能页面**：首页
   - **标签**：记账、语音记账、理财

### 9.3 审核检查清单

提交前确认以下全部 ✅：

- [ ] AppID 已替换为真实值
- [ ] 云环境 ID 已替换
- [ ] 9 个云函数全部部署成功（含 groupCRUD）
- [ ] 数据库集合已创建（expenses + users + budgets + ledgers）
- [ ] expenses/users/budgets 权限设为「仅创建者可读写」
- [ ] ledgers 权限设为「所有用户可读，仅创建者可写」
- [ ] 腾讯云语音识别已配置（可选）
- [ ] 隐私协议已配置（含麦克风权限说明）
- [ ] TabBar 图标已放置（或临时使用纯文字）
- [ ] 真机测试通过（语音识别 + 保存 + 统计 + 共享账本）
- [ ] 所有页面有完整内容，无空白页
- [ ] 无 console.error 报错

### 9.4 审核时间

- 首次提交：通常 **1-3 个工作日**
- 后续更新：通常 **1-24 小时**
- 审核结果通过微信公众平台站内信通知

---

## 第十步：正式发布

1. 审核通过后，在「版本管理」中找到已通过的版本
2. 点击「发布」
3. 选择「全量发布」
4. 小程序正式上线 🎉

### 发布后验证

1. 在微信中搜索你的小程序名称
2. 打开并完成一次完整的语音记账流程
3. 确认数据正常保存和统计

---

## 成本说明

| 资源 | 免费额度 | 本项目预估用量 |
|------|---------|---------------|
| 云函数调用 | 4万次/月 | 1000日活约3万次/月 |
| 数据库读写 | 5万次/月 | 1000日活约2万次/月 |
| 存储 | 2GB | 极少（纯文本数据） |
| 云函数流量 | 5GB/月 | 极少 |

> **结论**：日活 1000 以内完全免费。超过后按量付费，价格极低（参考：1万次云函数调用约 ¥0.5）。

---

## 后续运维

### 查看日志

云开发控制台 → 云函数 → 选择函数 → 日志

### 查看数据

云开发控制台 → 数据库 → 选择集合 → 数据记录

### 监控告警

云开发控制台 → 监控告警 → 设置阈值（如云函数错误率 > 5% 告警）

### 更新部署

1. 修改代码
2. 开发者工具上传新版本
3. 右键云函数 → 重新部署
4. 后台提交新版本审核

---

## 快速参考卡片

```
部署顺序：注册 → 配置 → 云开发 → 数据库 → 云函数 → ASR → 图标 → 隐私 → 测试 → 审核 → 发布

关键替换：
  project.config.json (根目录)  → appid
  miniprogram/app.js            → env (云环境ID)

数据库导入：
  database/expenses.json → expenses 集合
  database/users.json    → users 集合
  database/budgets.json  → budgets 集合
  database/ledgers.json  → ledgers 集合（导入后删除示例数据）

关键操作：
  云函数   → 右键「上传并部署：云端安装依赖」（共 9 个）
  数据库   → expenses/users/budgets 设为「仅创建者可读写」
             ledgers 设为「所有用户可读，仅创建者可写」
  ASR      → voiceToText 云函数环境变量配置 SecretId/SecretKey
  隐私     → 公众平台配置隐私保护指引
```
